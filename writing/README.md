# writing/

Plain-text plan for posts, not yet built out:

- Each post is a Markdown file (e.g. `writing/my-pkm-stack.md`) converted by hand
  or a small script into `writing/my-pkm-stack/index.html`, reusing `css/styles.css`
  and the same header/footer shell as `writing/index.html`.
- `writing/index.html` becomes the index listing posts with dates, replacing the
  "nothing published yet" placeholder.
- No build step / static site generator — keep it dependency-free, matching the
  rest of the site.

No posts exist yet; this is structure only.
