# moureauf.github.io

Personal homepage — plain HTML/CSS, no framework, no build step.

`writing/` is scaffolded for a future posts section; no posts yet.

Custom domain: francoismoureau.com (DNS + HTTPS already configured and live).
